package tests;

public class GorunupKaybolan {

    /*ReusableMethods.waitFor(10);
WebDriverWait wait1=new WebDriverWait(Driver.getDriver(),10);
ReusableMethods.getScreenshotWebElement("fogtaf",projePage.popMessage);
wait1.until(ExpectedConditions.visibilityOf(projePage.popMessage));
Assert.assertTrue(projePage.popMessage.isDisplayed());


     */
}
